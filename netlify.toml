[build]
    base = "zenduit-app"
    command = "npm run build --configuration production"
    publish = "dist/zenduit-forms/browser"

    [[redirects]]
    from = "/*"
    to = "/index.html"
    status  = 200
